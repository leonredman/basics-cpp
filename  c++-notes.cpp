// c++ notes file
#include <iostream>                           // library in and out
#include <string>                             // library to include strings data type
#include <cctype>                             // library to funcitons for characters types   
using namespace std;                          // allows use of namespace dont have to std:: on everything

int main() {                                  // entry point main - not really an int

  cout << "Hello World" << endl;

  return 0;                                   // return 0 ends program
}